# En producción el frontend se sirve desde el mismo origen que la API (Nest),
# que expone la API bajo el prefijo /api. Usamos una ruta relativa para que
# funcione sin importar el dominio donde se despliegue (EasyPanel, etc.).
VITE_API_URL=/api
